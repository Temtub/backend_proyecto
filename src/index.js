
//Bring the connection of the BD. Import mongoose from the file 
const { connection, mongoose } = require("./database/db")

//Import the model for User
const User = require("./database/models/Users")

    
